/**
 * @module Camera
 */
import { ClientApplication } from '../../client';
import { ActionSet } from '../helper';
import { Action, Options, OpenPayload, Payload } from './types';
/**
 * Camera
 */
export declare class Camera extends ActionSet {
    constructor(app: ClientApplication<any>, options?: Options);
    dispatch(action: Action.OPEN, payload: OpenPayload): Camera;
    dispatch(action: Action.CAPTURE, payload: Payload): Camera;
    private dispatchCameraAction;
}
export declare function create(app: ClientApplication<any>, options?: Options): Camera;
