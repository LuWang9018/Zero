import * as React from 'react';
import { WithContextTypes } from '../../../../types';
import { WithAppProviderProps } from '../../../AppProvider';
import { DropZoneContext } from '../../types';
export interface State {
    actionTitle?: string;
    actionHint?: string;
}
export interface Props {
    actionTitle?: string;
    actionHint?: string;
}
export declare type CombinedProps = Props & WithAppProviderProps & WithContextTypes<DropZoneContext>;
export declare class FileUpload extends React.Component<CombinedProps, State> {
    constructor(props: CombinedProps);
    updateStateFromProps(props: Props): void;
    componentWillReceiveProps(props: Props): void;
    componentWillMount(): void;
    render(): JSX.Element;
}
declare const _default: (React.ComponentClass<Props, React.ComponentState> & typeof FileUpload) | (React.StatelessComponent<Props> & typeof FileUpload);
export default _default;
