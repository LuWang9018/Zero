import * as React from 'react';
import { WithAppProviderProps } from '../../../AppProvider';
import { SortDirection } from '../../types';
export interface Props {
    testID?: string;
    height?: number;
    content?: React.ReactNode;
    contentType?: string;
    fixed?: boolean;
    truncate?: boolean;
    header?: boolean;
    total?: boolean;
    footer?: boolean;
    sorted?: boolean;
    sortable?: boolean;
    sortDirection?: SortDirection;
    defaultSortDirection?: SortDirection;
    onSort?(): void;
}
export declare type CombinedProps = Props & WithAppProviderProps;
declare function Cell({ height, content, contentType, fixed, truncate, header, total, footer, sorted, sortable, sortDirection, defaultSortDirection, polaris: { intl: { translate }, }, onSort, }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props, React.ComponentState> & typeof Cell;
export default _default;
