import * as React from 'react';
import { WithAppProviderProps } from '../AppProvider';
export interface Props {
    /** Content to display in the tag */
    children?: string;
    /** Disables the tag  */
    disabled?: boolean;
    /** Callback when tag is removed */
    onRemove?(): void;
}
export declare type CombinedProps = Props & WithAppProviderProps;
declare function Tag({ children, disabled, onRemove, polaris: { intl }, }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props, React.ComponentState> & typeof Tag;
export default _default;
