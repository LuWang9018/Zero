import * as React from 'react';
export interface Props {
    title?: React.ReactNode;
    children?: React.ReactNode;
    subdued?: boolean;
    fullWidth?: boolean;
}
export default function Section({ children, title, subdued, fullWidth }: Props): JSX.Element;
