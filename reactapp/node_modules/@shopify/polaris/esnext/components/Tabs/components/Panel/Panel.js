import * as React from 'react';
import styles from '../../Tabs.scss';
export default function Panel({ id, tabID, children }) {
    return (<div className={styles.Panel} id={id} role="tabpanel" aria-labelledby={tabID} tabIndex={-1}>
      {children}
    </div>);
}
